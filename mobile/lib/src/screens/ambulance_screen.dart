import 'package:flutter/material.dart';

// TODO: Implement ambulance facility UI (emergency contacts, call integration)
class AmbulanceScreen extends StatelessWidget {
  const AmbulanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ambulance Facility')),
      body: const Center(child: Text('Ambulance facility TBD')),
    );
  }
}
