import 'package:flutter/material.dart';

// TODO: Implement simple Q&A chatbot with FAQ list
class ChatbotFAQScreen extends StatelessWidget {
  const ChatbotFAQScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FAQ Chatbot')),
      body: const Center(child: Text('Chatbot TBD')),
    );
  }
}
